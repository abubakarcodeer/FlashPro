<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlashPro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="cardBackgroundColor">@color/white</item>
        <item name="viewColor">@color/view_color_light</item>
        <item name="seekbar_thumb_color">@color/black</item>
        <item name="seekbar_track_color">@color/black</item>
        <item name="dialogViewColor">@color/black</item>
        <item name="dialogColor">@color/white</item>
        <item name="seekbar_track_background_color">@color/black</item>
        <item name="navigation_background">@color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.FlashPro" parent="Base.Theme.FlashPro" />
</resources>