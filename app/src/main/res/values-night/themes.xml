<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlashPro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
         <item name="colorPrimary">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="viewColor">@color/view_color_dark</item>
        <item name="dialogViewColor">@color/white</item>
        <item name="dialogColor">@color/card_background</item>
        <item name="seekbar_thumb_color">@color/seekbar_color</item>
        <item name="seekbar_track_color">@color/seekbar_color</item>
        <item name="navigation_background">@color/switch_background</item>
        <item name="seekbar_track_background_color">@color/white</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>